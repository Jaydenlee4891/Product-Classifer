# Install torch separately first, matching your CUDA version: https://pytorch.org
# GTX 1660 Ti note: Turing, compute capability 7.5 — no bf16, no FlashAttention-2.
# Use fp16 (recall.py does this automatically on CUDA) and PyTorch's built-in SDPA.

sentence-transformers>=3.0.0
transformers>=4.51.0
pandas>=2.0.0
pyarrow>=14.0.0
numpy>=1.24.0
matplotlib>=3.7.0
tqdm>=4.66.0

# only needed for `label_docs.py --describe`
anthropic>=0.40.0
scikit-learn>=1.3.0

# --- serving layer (src/serve) ---
# LangChain/LangGraph 1.x. create_agent and the 0.x LCEL chain APIs are a different
# surface; pin the major so a tutorial-era example cannot be pasted in silently.
langgraph>=1.0,<2
langchain-core>=1.0,<2
langchain-anthropic>=1.0,<2      # provider 'anthropic'
fastapi>=0.115
uvicorn[standard]>=0.30
# the Django front end (src/serve_django) — an alternative to the FastAPI one, not an
# addition to it; neither is needed to run the offline pipeline
django>=5.0

# provider 'ollama', local arm only — not needed for the cached or Anthropic paths
# langchain-ollama>=1.0,<2
